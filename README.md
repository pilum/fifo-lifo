# FIFO and LIFO Data Structures Exercise

> **Generated by Cascade (SWE-1.5)** - This exercise was created for learning purposes upon request to help understand FIFO and LIFO data structures through hands-on implementation.

## Learning Objectives
- Understand the difference between FIFO (First In, First Out) and LIFO (Last In, First Out)
- Implement both data structures from scratch
- Practice with basic operations: enqueue/dequeue (FIFO) and push/pop (LIFO)

## Theory Recap

### FIFO (Queue)
- **First In, First Out** - like a line at a grocery store
- The first element added is the first one to be removed
- Operations: `enqueue()` (add to back), `dequeue()` (remove from front), `peek()` (look at front)

### LIFO (Stack)
- **Last In, First Out** - like a stack of plates
- The last element added is the first one to be removed
- Operations: `push()` (add to top), `pop()` (remove from top), `peek()` (look at top)

---

## Exercise 1: Implement a FIFO Queue

Create a `FIFOQueue` class with the following methods:

```python
class FIFOQueue:
    def __init__(self):
        """Initialize an empty queue"""
        pass
    
    def enqueue(self, item):
        """Add an item to the back of the queue"""
        pass
    
    def dequeue(self):
        """Remove and return the item from the front of the queue.
        Returns None if queue is empty."""
        pass
    
    def peek(self):
        """Return the item at the front without removing it.
        Returns None if queue is empty."""
        pass
    
    def is_empty(self):
        """Return True if queue is empty, False otherwise"""
        pass
    
    def size(self):
        """Return the number of items in the queue"""
        pass
```

### Test your FIFO Queue:
```python
# Test cases
queue = FIFOQueue()
print(queue.is_empty())  # Should print: True

queue.enqueue("A")
queue.enqueue("B")
queue.enqueue("C")
print(queue.size())      # Should print: 3
print(queue.peek())      # Should print: A

print(queue.dequeue())   # Should print: A
print(queue.dequeue())   # Should print: B
print(queue.size())      # Should print: 1
print(queue.peek())      # Should print: C
```

---

## Exercise 2: Implement a LIFO Stack

Create a `LIFOStack` class with the following methods:

```python
class LIFOStack:
    def __init__(self):
        """Initialize an empty stack"""
        pass
    
    def push(self, item):
        """Add an item to the top of the stack"""
        pass
    
    def pop(self):
        """Remove and return the item from the top of the stack.
        Returns None if stack is empty."""
        pass
    
    def peek(self):
        """Return the item at the top without removing it.
        Returns None if stack is empty."""
        pass
    
    def is_empty(self):
        """Return True if stack is empty, False otherwise"""
        pass
    
    def size(self):
        """Return the number of items in the stack"""
        pass
```

### Test your LIFO Stack:
```python
# Test cases
stack = LIFOStack()
print(stack.is_empty())  # Should print: True

stack.push("X")
stack.push("Y")
stack.push("Z")
print(stack.size())      # Should print: 3
print(stack.peek())      # Should print: Z

print(stack.pop())       # Should print: Z
print(stack.pop())       # Should print: Y
print(stack.size())      # Should print: 1
print(stack.peek())      # Should print: X
```

---

## Exercise 3: Real-world Scenario

**Task:** Simulate a print queue system using your FIFO queue.

1. Create print jobs with different priorities (1-5, where 1 is highest priority)
2. Add jobs to the queue
3. Process jobs in FIFO order
4. Show the order in which jobs get printed

```python
# Example structure
class PrintJob:
    def __init__(self, name, priority, pages):
        self.name = name
        self.priority = priority
        self.pages = pages
    
    def __str__(self):
        return f"Job: {self.name}, Priority: {self.priority}, Pages: {self.pages}"

# Your implementation here
```

---

## Exercise 4: Challenge - Browser History

**Task:** Implement a simple browser history system using your LIFO stack.

1. When user visits a new page, push it to the stack
2. When user clicks "back", pop from the stack
3. Show current page and history
4. Handle edge cases (empty history, etc.)

---

## Bonus Questions

1. What happens if you try to dequeue from an empty FIFO queue?
2. What happens if you try to pop from an empty LIFO stack?
3. How would you modify your FIFO queue to handle priority items?
4. Can you think of 3 real-world examples of FIFO and 3 of LIFO?

---

## Implementation Tips

- Use Python lists as your underlying data structure
- For FIFO: consider using `append()` for enqueue and `pop(0)` for dequeue
- For LIFO: consider using `append()` for push and `pop()` for pop
- Add error handling for edge cases
- Test each method individually before moving to the next exercise

Good luck! 🚀
